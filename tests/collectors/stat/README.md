# Stat Collector Tests

Tests for the stat collector group, which includes:
- **User Tables**: Table-level statistics from `pg_stat_user_tables`

## Running Tests

### Run all stat collector tests
```bash
cargo test --test collectors_tests stat
